<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">


    <EditText android:id="@+id/activity_formulario_patrimonio"
        android:layout_width="match_parent"
        android:layout_margin="8dp"
        android:hint="Patrimônio"
        android:inputType="textCapWords"
        android:layout_height="wrap_content"/>
    
    <EditText android:id="@+id/activity_formulario_patrimonio_nome"
        android:layout_width="match_parent"
        android:layout_margin="8dp"
        android:hint="Nome"
        android:inputType="textCapWords"
        android:layout_height="wrap_content"/>

    <EditText
        android:id="@+id/activity_formulario_patrimonio_endereco"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:hint="Endereço"
        android:inputType="textCapWords" />

    <EditText android:id="@+id/activity_formulario_patrimonio_telefone"
        android:layout_width="match_parent"
        android:layout_margin="8dp"
        android:hint="Telefone"
        android:inputType="phone"
        android:layout_height="wrap_content"/>

    <EditText android:id="@+id/activity_formulario_patrimonio_email"
        android:layout_width="match_parent"
        android:layout_margin="8dp"
        android:hint="E-mail"
        android:inputType="textEmailAddress"
        android:layout_height="wrap_content"/>

    <Button android:id="@+id/activity_formulario_patrimonio_botao_salvar"
        android:layout_width="match_parent"
        android:layout_margin="8dp"
        android:text="Salvar"
        android:layout_height="wrap_content"/>

</LinearLayout>